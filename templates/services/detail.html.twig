{% extends "base.html.twig" %}
{% block body %}
	{{ include('resource/_nav.html.twig') }}
	<div class="container"></div>
	<div class=" line">
		<h1 class="big-title line">{{serviceVews}}</h1>
		<div class="text line flexible">
			<p class="p">description</p>
		</div>
		<a class="link btn btn-advanced" href=" {{ path('app_contact')}}">Contactez-nous</a>
	</div>
{% endblock body %}
